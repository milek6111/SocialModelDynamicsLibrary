package pl.edu.agh.models.enums;

/**
 * Enum representing the strategy for selecting agents in the model.
 * SEQUENTIAL - Agents are selected in a sequential order.
 * RANDOM - Agents are selected randomly.
 */
public enum AgentSelection {
    SEQUENTIAL,
    RANDOM
}