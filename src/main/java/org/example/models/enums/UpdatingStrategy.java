package org.example.models.enums;

/**
 * Enum representing the strategy for updating opinions in the model.
 * ONE_STEP - Opinions are updated one step at a time.
 * N_TIMES - Opinions are updated N times.
 */
public enum UpdatingStrategy {
    ONE_STEP,
    N_TIMES
}